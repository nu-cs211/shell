# CS 211 tcsh configuration

if ( -e /home/cs211/pub/etc/profile.csh ) then
    source /home/cs211/pub/etc/profile.csh
endif

if ( -e ~/.cshaliases ) then
    source ~/.cshaliases
endif

# vim: se ft=tcsh:
