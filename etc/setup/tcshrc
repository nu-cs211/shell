# CS 211 tcsh configuration

if ( -e ~cs211/pub/etc/bootstrap-wi2021 ) then
    source ~cs211/pub/etc/bootstrap-wi2021
endif

if ( -e ~/.cshaliases ) then
    source ~/.cshaliases
endif

# vim: se ft=tcsh:
